nepal-pilots.response.hotosm.org {
	root * /srv/flights

	basic_auth {
		admin {env.BOARD_PASSWORD_HASH}
	}

	# Keep serving stale data; the dashboard displays its own warning.
	file_server
	header Cache-Control "no-cache"
	header X-Robots-Tag "noindex, nofollow"
}
